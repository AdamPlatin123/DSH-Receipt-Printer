{
  "manifestVersion": "0.15",
  "identity": {
    "name": "dsh-thermal-receipt",
    "package": "dsh-thermal-receipt",
    "version": "0.1.1",
    "description": "Print an ESC/POS 58mm thermal receipt on every DSH turn end (works in dsh-tui and dsh-desktop via the core session/event seam)"
  },
  "facets": {
    "host": {
      "entry": "./lib/index.js",
      "apiVersion": "0.15"
    }
  },
  "requires": {
    "contracts": []
  },
  "permissions": [
    {
      "kind": "process.spawn",
      "reason": "Spawn the bundled Python bridge (vendor/thermal_bridge.py) to send raw ESC/POS bytes to the local thermal printer via Win32 spooler"
    }
  ],
  "subscriptions": [
    "session/event",
    "session/disposed",
    "agent/status"
  ]
}
